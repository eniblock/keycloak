ingress:
  host: "kc.localhost"
  enabled: true
replicas: 1
metrics:
  enabled: true
global:
  dev: true
configurator:
  secrets:
    realm_id: foo
    array:
      - foo
      - bar
    map:
      foo: bar
